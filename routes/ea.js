const express = require('express'); const router = express.Router(); const multer = require('multer'); router.post('/upload', (req, res) => { res.json({ message: 'EA upload', disclaimer: 'BrokersSync is NOT liable' }); }); router.get('/my-eas', (req, res) => { res.json({ eas: [] }); }); router.delete('/:eaId', (req, res) => { res.json({ message: 'Deleted' }); }); module.exports = router;
